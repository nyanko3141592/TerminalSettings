# 開発環境構成

macOS (Darwin 25.2.0 / Apple Silicon)

## ターミナル

| ツール | バージョン | 用途 |
|--------|-----------|------|
| Ghostty | 1.3.0 | 高速ターミナルエミュレータ（透過設定済み） |
| tmux | 3.6a | ターミナルマルチプレクサ（prefix: C-a） |

## シェル (Zsh)

| ツール | バージョン | 用途 |
|--------|-----------|------|
| zsh | 5.9 (system) | デフォルトシェル |
| zsh-autosuggestions | 0.7.1 | fish風の履歴ベース入力候補（グレー文字表示） |
| zsh-syntax-highlighting | 0.8.0 | 入力中のコマンドをリアルタイムハイライト |
| zsh-completions | 0.35.0 | 追加の補完定義 |
| Starship | 1.24.2 | カスタマイズ可能なプロンプト |

### 設定ファイル

| ファイル | 用途 |
|---------|------|
| `~/.zshrc` | シェル設定本体（エイリアス・プラグイン・ツール連携） |
| `~/.config/starship.toml` | プロンプトの見た目設定 |
| `~/.config/ghostty/config` | ターミナル設定（透過・テーマ・フォント等） |
| `~/.tmux.conf` | tmux設定（prefix・分割・ステータスバー等） |

### エイリアス

既存コマンド（cat, grep, find, cd, diff）は上書きしない方針。`bat`, `rg`, `fd`, `z`, `delta` をそのまま使う。

| エイリアス | 実体 | 説明 |
|-----------|------|------|
| `ls` | `eza --icons` | アイコン付きファイル一覧 |
| `ll` | `eza -la --icons --git` | 詳細一覧（git status付き） |
| `lt` | `eza --tree --icons --level=2` | ツリー表示 |
| `g` | `git` | git短縮 |
| `gs` | `git status` | ステータス確認 |
| `gd` | `git diff` | 差分表示 |
| `gl` | `git log --oneline -20` | 直近20件のログ |
| `gp` | `git push` | プッシュ |
| `lg` | `lazygit` | Git TUI起動 |
| `c` | `clear` | 画面クリア |

## ランタイム

| ツール | バージョン | 用途 |
|--------|-----------|------|
| Node.js | v25.8.0 | JavaScript実行環境 |
| Python | 3.9.6 (system) | Python実行環境（システム標準） |
| Python | 3.13.12 (Homebrew) | Python実行環境（Homebrew版） |
| Git | 2.50.1 (Apple Git) | バージョン管理 |

## パッケージ管理

| ツール | バージョン | 用途 |
|--------|-----------|------|
| Homebrew | 5.1.0 | macOS標準パッケージ管理 |
| pnpm | 10.32.0 | Node.jsパッケージ管理（npm/yarnより高速・推奨） |
| uv | 0.10.9 | Pythonパッケージ管理（pip/poetryより高速） |

## CLI検索・ナビゲーション

| ツール | バージョン | 用途 |
|--------|-----------|------|
| ripgrep (`rg`) | 15.1.0 | 高速文字列検索（grepの代替） |
| fd | 10.4.1 | 高速ファイル検索（findの代替） |
| fzf | 0.70.0 | 曖昧検索（インタラクティブフィルタ） |
| zoxide | 0.9.9 | 高機能ディレクトリ移動（cdの代替） |

## ファイル表示

| ツール | バージョン | 用途 |
|--------|-----------|------|
| bat | 0.26.1 | シンタックスハイライト付きcat |
| eza | 0.23.4 | ツリー表示対応ls |
| tree | 2.3.1 | ディレクトリ構造をツリー表示 |

## Git関連

| ツール | バージョン | 用途 |
|--------|-----------|------|
| git-delta | 0.18.2 | 視認性の高いgit diff表示 |
| gh | 2.87.3 | GitHub CLI |
| lazygit | 0.60.0 | Git TUI |

## システムモニタリング

| ツール | バージョン | 用途 |
|--------|-----------|------|
| btop | 1.4.6 | モダンなプロセス/リソースモニター（topの代替） |

## データ加工

| ツール | バージョン | 用途 |
|--------|-----------|------|
| jq | 1.7.1 | JSON加工 |

## AI / エージェント

| ツール | バージョン | 用途 |
|--------|-----------|------|
| Claude Code | 2.1.76 | Anthropic公式CLIエージェント |

## iOS / Xcode

| ツール | バージョン | 用途 |
|--------|-----------|------|
| XcodeGen | 2.45.2 | Xcodeプロジェクト生成（YAML定義） |

## ユーティリティ

| ツール | バージョン | 用途 |
|--------|-----------|------|
| Maccy | 2.6.1 | クリップボードマネージャー |

## 入力

| ツール | 用途 |
|--------|------|
| azooKey | 日本語IME |
